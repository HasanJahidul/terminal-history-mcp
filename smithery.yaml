runtime: "container"

startCommand:
  type: stdio
  configSchema:
    type: object
    required: []
    properties: {}
    additionalProperties: false
  exampleConfig: {}

build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
